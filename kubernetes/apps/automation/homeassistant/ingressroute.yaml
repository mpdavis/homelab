apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homeassistant
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`homeassistant.${DOMAIN}`)
      kind: Rule
      services:
        - name: homeassistant
          port: 8123
  tls: {}
