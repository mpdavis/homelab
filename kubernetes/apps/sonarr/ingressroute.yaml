apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.mpdavis.com`)
      kind: Rule
      services:
        - name: sonarr
          port: 8989
  tls: {}
