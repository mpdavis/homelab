apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: coding-agent
  labels:
    homelab.mpdavis.com/exposure: tailnet
spec:
  entryPoints:
    - tailnet
  routes:
    - match: Host(`code.${DOMAIN}`)
      kind: Rule
      middlewares:
        - name: authentik-forward-auth
          namespace: authentik
      services:
        - name: coding-agent
          port: 3001
  tls: {}
