apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: paperless
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`paperless.mpdavis.com`)
      kind: Rule
      services:
        - name: paperless
          port: 8000
  tls: {}
