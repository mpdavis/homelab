apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: trek
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`trek.${DOMAIN}`)
      kind: Rule
      services:
        - name: trek
          port: 3000
  tls: {}
