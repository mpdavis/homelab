apiVersion: apps/v1
kind: Deployment
metadata:
  name: teamarr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: teamarr
  template:
    metadata:
      labels:
        app: teamarr
    spec:
      containers:
        - name: teamarr
          image: ghcr.io/pharaoh-labs/teamarr:2.14.0
          securityContext:
            readOnlyRootFilesystem: true
          ports:
            - containerPort: 9195
              name: http
          env:
            - name: TZ
              value: ${TZ}
          volumeMounts:
            - name: data
              mountPath: /app/data
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: teamarr-data
        - name: tmp
          emptyDir: {}
