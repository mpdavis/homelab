apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minecraft
spec:
  interval: 30m
  chart:
    spec:
      chart: minecraft
      version: "~5.2.0"
      sourceRef:
        kind: HelmRepository
        name: minecraft
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
      strategy: rollback
    crds: CreateReplace
  values:
    image:
      repository: itzg/minecraft-server
      # Pinned to a stable, immutable date tag (digest
      # sha256:e8640538dac5d54c2838d57fa9641e735ad0cf2b71fb0e8a68da3b542a315749).
      # Controls the itzg tooling + Java runtime (Java 25, required since
      # Minecraft's LATEST release now ships class file version 69); the
      # Minecraft game version is set separately via minecraftServer.version
      # below.
      tag: "2026.9.2-java25"
      pullPolicy: IfNotPresent
    minecraftServer:
      # Accepting the Minecraft EULA (https://www.minecraft.net/en-us/eula) is
      # required for the server to start.
      eula: "TRUE"
      # LATEST tracks the newest Minecraft release; pin to an explicit version
      # (e.g. "1.21.4") if world/mod compatibility must be held stable.
      version: "LATEST"
      difficulty: normal
      memory: 2048M
      # Minecraft speaks its own TCP protocol on 25565 — not HTTP — so it is
      # exposed via a MetalLB LoadBalancer rather than a Traefik IngressRoute.
      serviceType: LoadBalancer
      # loadBalancerIP left unset — MetalLB assigns one from lan-pool. Point the
      # minecraft.mpdavis.com DNS record at the assigned IP (see PR notes).
      rcon:
        enabled: false
    resources:
      requests:
        cpu: 500m
        memory: 2Gi
      limits:
        memory: 3Gi
    persistence:
      dataDir:
        enabled: true
        existingClaim: minecraft
