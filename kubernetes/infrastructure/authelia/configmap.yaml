apiVersion: v1
kind: ConfigMap
metadata:
  name: authelia-config
data:
  configuration.yml: |
    server:
      address: 'tcp4://:9091'

    log:
      level: info

    theme: auto

    totp:
      disable: false
      issuer: mpdavis.com

    authentication_backend:
      file:
        path: /config/users_database.yml
        password:
          algorithm: argon2
          argon2:
            variant: argon2id
            iterations: 3
            memory: 65536
            parallelism: 4
            key_length: 32
            salt_length: 16

    access_control:
      default_policy: deny
      rules:
        - domain: '*.mpdavis.com'
          policy: one_factor

    session:
      name: authelia_session
      cookies:
        - domain: mpdavis.com
          authelia_url: https://auth.mpdavis.com
          default_redirection_url: https://auth.mpdavis.com

    regulation:
      max_retries: 5
      find_time: 2m
      ban_time: 5m

    storage:
      local:
        path: /data/db.sqlite3

    notifier:
      disable_startup_check: false
      filesystem:
        filename: /data/notification.txt
