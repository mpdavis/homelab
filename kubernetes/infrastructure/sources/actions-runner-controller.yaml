apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: actions-runner-controller
  namespace: flux-system
spec:
  interval: 1h
  type: oci
  # Registry root; the chart name (gha-runner-scale-set-controller /
  # gha-runner-scale-set) is set per-HelmRelease.
  url: oci://ghcr.io/actions/actions-runner-controller-charts
