{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "flux": {
    "managerFilePatterns": [
      "/kubernetes/.+\\.ya?ml$/"
    ]
  },
  "kubernetes": {
    "managerFilePatterns": [
      "/kubernetes/.+\\.ya?ml$/"
    ]
  },
  "customManagers": [
    {
      "description": "Bump tool versions pinned as annotated env vars in GitHub workflows. The github-actions manager only sees `uses:` refs, so a binary downloaded by URL (e.g. kubeconform in render-validate.yml) would otherwise silently rot.",
      "customType": "regex",
      "managerFilePatterns": [
        "/^\\.github/workflows/.+\\.ya?ml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>[^\\s]+)\\n\\s+[A-Z_]+: \"(?<currentValue>[^\"]+)\""
      ]
    },
    {
      "description": "Bump tool versions pinned in Ansible role defaults — the devbox agent CLIs (claude-code, opencode) and its kubectl/flux/gh binaries. Ansible vars are invisible to every built-in manager, so these rot silently otherwise; same problem, same fix as the workflows manager above.",
      "customType": "regex",
      "managerFilePatterns": [
        "/^bootstrap/ansible/roles/.+/defaults/main\\.ya?ml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>[^\\s]+)\\n[a-z0-9_]+: \"(?<currentValue>[^\"]+)\""
      ]
    }
  ],
  "packageRules": [
    {
      "description": "Major version bumps usually require migration steps. Gate them behind manual approval on the dependency dashboard instead of opening PRs automatically.",
      "matchUpdateTypes": [
        "major"
      ],
      "dependencyDashboardApproval": true
    },
    {
      "description": "CRD apiVersion bumps (kubernetes-api datasource, e.g. ClusterSecretStore -> external-secrets.io/v1) are coupled to operator/CRD upgrades and must never be applied on their own. Require manual approval.",
      "matchDatasources": [
        "kubernetes-api"
      ],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Review all External Secrets changes together in one PR: the Helm chart and the ClusterSecretStore/ExternalSecret/SecretStore apiVersions move in lockstep. The v0.x -> v2.x upgrade needed a coordinated CRD conversion-webhook cleanup and storage migration, so gate this group behind manual approval.",
      "matchPackageNames": [
        "external-secrets",
        "ClusterSecretStore",
        "ClusterExternalSecret",
        "ExternalSecret",
        "SecretStore",
        "PushSecret"
      ],
      "groupName": "external-secrets",
      "dependencyDashboardApproval": true
    },
    {
      "description": "Auto-merge the bumps that carry the least migration risk: digest re-pins, patch releases, and minor releases (assumed backwards-compatible under semver). Uses GitHub's native auto-merge (platformAutomerge), so the PR only lands once every REQUIRED status check on main's ruleset passes. NOTE: a check that merely runs is not enough — it must be marked required in the 'Restrictions for main' ruleset, otherwise auto-merge lands the PR immediately with no gate at all. `verify` (image-pin-check) is the check worth requiring here.",
      "matchUpdateTypes": [
        "digest",
        "patch",
        "minor"
      ],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "Never auto-merge CRD apiVersion bumps, even a patch-shaped one. Dashboard approval only controls whether the PR opens — it does not imply the diff was reviewed. Keep this and the rule below AFTER the automerge rule: Renovate applies packageRules in order and the last write to `automerge` wins.",
      "matchDatasources": [
        "kubernetes-api"
      ],
      "automerge": false
    },
    {
      "description": "Never auto-merge the external-secrets group — chart and CRD apiVersions move in lockstep and need to be read together.",
      "matchPackageNames": [
        "external-secrets",
        "ClusterSecretStore",
        "ClusterExternalSecret",
        "ExternalSecret",
        "SecretStore",
        "PushSecret"
      ],
      "automerge": false
    },
    {
      "description": "WHY: Listenarr only ships canary-X.Y.Z prereleases, and Renovate's default docker versioning can't parse the 'canary-' prefix, so this regex versioning treats 'canary' as a prerelease marker to keep canary tracked now while auto-switching to a stable tag once one exists. WHEN TO REMOVE: once the image is migrated to a stable (non-canary) tag, as default docker versioning then handles plain X.Y.Z tags on its own.",
      "matchPackageNames": ["ghcr.io/listenarrs/listenarr"],
      "versioning": "regex:^(?:v)?(?:(?<prerelease>canary)-)?(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)$"
    },
    {
      "description": "n8n ships each weekly minor as a beta (docker tag `next`) under a plain X.Y.Z tag, then promotes it to `latest` when the following minor goes to beta. Semver can't tell the two apart, so without this the minor automerge rule would deploy betas the day they're cut. A week's age is roughly when a release becomes stable.",
      "matchPackageNames": ["n8nio/n8n"],
      "minimumReleaseAge": "7 days"
    }
  ]
}
