{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "if": "Bash(gh pr create *)",
            "prompt": "A PR was just created. Extract the PR number from the tool output and run /check-pr with that number to verify it is mergeable (no merge conflicts, required checks passing). If there are merge conflicts, fix them."
          }
        ]
      }
    ]
  }
}
