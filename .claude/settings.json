{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "git fetch origin main",
            "type": "command"
          }
        ],
        "matcher": ""
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "if": "Bash(gh pr create *)",
            "prompt": "A PR was just created. Extract the PR number from the tool output and start monitoring it by running `/loop /babysit-pr <number>` (replace <number> with the actual PR number). This will continuously monitor build checks, handle review comments, and manage the worktree lifecycle once the PR merges."
          }
        ]
      }
    ]
  }
}
