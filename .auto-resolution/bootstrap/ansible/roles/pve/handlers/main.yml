---
- name: update apt cache
  ansible.builtin.apt:
    update_cache: true

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: apply sysctls
  ansible.builtin.command: sysctl --system
  changed_when: true
