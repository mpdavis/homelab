apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ntfy
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`ntfy.${DOMAIN}`)
      kind: Rule
      services:
        - name: ntfy
          port: 80
  tls: {}
