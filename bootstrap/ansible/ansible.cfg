[defaults]
inventory = inventory
roles_path = roles
host_key_checking = false

[inventory]
# inventory/network.yml fails to parse without bootstrap/network.yaml; stop
# rather than fall back to connecting by hostname.
any_unparsed_is_failed = true
