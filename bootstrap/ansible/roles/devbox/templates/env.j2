# Managed by Ansible (roles/devbox) — edits here are overwritten. Mode 0600.
{% if devbox_claude_oauth_token | default('') | length > 0 %}
export CLAUDE_CODE_OAUTH_TOKEN={{ devbox_claude_oauth_token }}
{% endif %}
