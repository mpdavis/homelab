# Managed by Ansible (roles/devbox) — edits here are overwritten.
# The account is key-only and has no password, so prompting would make sudo
# unusable rather than safer.
{{ devbox_user }} ALL=(ALL) NOPASSWD:ALL
