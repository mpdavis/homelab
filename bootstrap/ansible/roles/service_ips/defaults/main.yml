---
# Must match the netplan ID cloud-init writes to 50-cloud-init.yaml. Netplan
# concatenates `addresses` across files for the same ID, so this drop-in adds
# to the primary address instead of replacing it.
service_ips_interface: eth0

# CIDR strings, e.g. ["192.0.2.10/24"].
service_ips: []
